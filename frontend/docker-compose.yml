version: '3.8'

services:
  frontend:
    build: 
      context: ./frontend
    image: ${DOCKER_USERNAME}/tariffic-frontend:latest
    container_name: CSD-frontend
    ports:
      - "80:80"
    restart: unless-stopped
